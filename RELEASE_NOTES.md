## What's New
- Tasks created in auto-spawn columns now automatically launch an agent session
- Improved first-launch experience with a welcome overlay and git repository detection
- New Notifications tab in settings with terminal options, idle timeout configuration, and window restore behavior

## Bug Fixes
- Fixed welcome overlay flashing during HMR config store re-sync
- All IPC-backed stores now re-sync correctly after Vite HMR updates
- Bottom panel no longer switches focus when the Task Detail dialog is open
- Context window percentage now includes output tokens for accurate reporting
